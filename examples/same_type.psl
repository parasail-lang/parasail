func Run() is
   type Answer is PSL::Core::Enum<[#no, #idk, #yes]>;
   type Answers is Array<Univ_Character, Answer>;
   const No : Answers := [#no => 'n', #idk => 'i', #yes => 'y'];
   Println(No[#yes]);
end func Run;
